{% extends 'index/index.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block body %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ reclamation.title }}</td>
            </tr>
            <tr>
                <th>Body</th>
                <td>{{ reclamation.body }}</td>
            </tr>
            <tr>
                <th>Subject</th>
                <td>{{ reclamation.subject }}</td>
            </tr>
            <tr>
                <th>UserId</th>
                <td>{{ reclamation.userId }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reclamation.date ? reclamation.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Etatprobleme</th>
                <td>{{ reclamation.etatprobleme }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reclamation_index') }}">
        <p class="footer_contact-form-submit topmargin_30">
            <button type="submit" id="footer_contact_form_submit" name="contact_submit" class="theme_button color1 wide_button">Retour à la liste </button>
        </p>
    </a>

    <a href="{{ path('reclamation_edit', {'id': reclamation.id}) }}">
        <p class="footer_contact-form-submit topmargin_30">
            <button type="submit" id="footer_contact_form_submit" name="contact_submit" class="theme_button color1 wide_button">Modifier la reclamation </button>
        </p>
    </a>

    {{ include('reclamation/_delete_form.html.twig') }}
{% endblock %}
