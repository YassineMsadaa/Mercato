{% extends 'basebackadmin.html.twig' %}



{% block body %}
    <h1>Users</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ user.id }}</td>
        </tr>
        <tr>
            <th>Name of user</th>
            <td>{{ user.Username }}</td>
        </tr>

            <th>Email Adress</th>
            <td>{{ user.emailadresse }}</td>
        </tr>
        <tr>
            <th>Password</th>
            <td>{{ user.password }}</td>
        </tr>
        <tr>
            <th>questions</th>
            <td>{{ user.s_ques }}</td>
        </tr>
        <tr>
            <th>Status</th>
            <td>{{ user.type }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('companyeditm', {'id': user.id}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}


{% endblock %}